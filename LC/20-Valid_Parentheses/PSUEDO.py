# 1
Stack : callable(collection TYPE, SIZE, CONSTRAINTS:Optional);
## stack.PUSH (x) NOT GOOD

# 2
Stack : callable(collection TYPE, SIZE);

def is_paren(char):
    if char in CONSTRAINTS:
        return true
    else 
        return false

def is_pushable(data:str):
    flags = [0,0,0];
    stack:Stack, 
    stack = STACK(MAX_LEN);
    for key in data:
        char = ord(key);
        if not is_paren(ch):
            RET FALSE

        if key == ('(' or ')') and not flags[0]:
            PUSH KEY to STACK
        elif key '[':
            PUSH
        elif key '{':
            PUSH
        else:
            RET FALSE
    retrun TRUE
                    

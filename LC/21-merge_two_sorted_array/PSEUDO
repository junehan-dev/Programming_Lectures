S1 = [1,2,4];
S2 = [1,3,4];

MAIN
----
slist_q = q(1);	// 1. size = 1 FOR head_node.

LOOP(CMP_AND_LINK)

IF NOT s1 AND NOT s2

RETURN q.deque()

CMP AND LINK
------------
head1 < head2 CMP
TRUE:
	q.enque(head1)
	head1 = head1.next
FALSE:
	q.enque(head2)
	head2 = head1.next
	return True;

